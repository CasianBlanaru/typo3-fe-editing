# Add our content element to the new content element wizard
mod.wizards.newContentElement.wizardItems.common {
  elements {
    pc_demo {
      iconIdentifier = ext-pixelcoda-fe-editor
      title = PixelCoda Demo (editierbar)
      description = Einfaches Demo-Element mit Frontend-Edit-Markup
      tt_content_defValues {
        CType = pc_demo
      }
    }
  }
  show := addToList(pc_demo)
}
